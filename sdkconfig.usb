# USB configuration for ESP32-S3 Vision - ENABLED via TinyUSB CDC
# ESP32-S3 has native USB OTG, use it for MicroPython USB CDC REPL
# Required because no USB-UART bridge on hardware
CONFIG_USB_CDC_BOOT_DEFAULT=n
CONFIG_USB_CDC_ENABLED=n
CONFIG_USB_OTG_SUPPORTED=y
CONFIG_TINYUSB_ENABLED=y